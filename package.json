{
  "name": "oura-worker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv namespace create OURA_KV"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
